# Esta carpeta contiene los archivos JSON de persistencia

Los archivos `products.json` y `carts.json` se crean automáticamente al iniciar el servidor.

Si necesitas crearlos manualmente:
- products.json: []
- carts.json: []
