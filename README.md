# Entrega 1 - Backend (Products & Carts)

API simple en Node.js + Express con persistencia en archivos JSON.

Cómo usar:

1. Instalar dependencias:

   npm install

2. Iniciar servidor:

   node src/app.js

3. Endpoints principales:

   - GET /api/products
   - GET /api/products/:pid
   - POST /api/products
   - PUT /api/products/:pid
   - DELETE /api/products/:pid
   - POST /api/carts
   - GET /api/carts/:cid
   - POST /api/carts/:cid/product/:pid

Probar con Postman, curl o tu cliente favorito.
